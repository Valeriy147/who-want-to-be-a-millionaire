export enum GameActions {
  SET_QUESTIONS = '[Game] Save questions',
  SET_ACTUAL_QUESTION = '[Game] Set actual question',
  SET_GAME_SETTINGS = '[Game] Set game settings',
  NEXT_STEP = '[Game] Next step',
  NEW_GAME = '[Game] New game',
  START_OVER = '[Game] Start over'
}
